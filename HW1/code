#define CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
/*
	int num1, num2, num3;
	int min;
	printf("3���� ������ �Է��Ͻÿ�:\n");
	scanf_s("%d %d %d", &num1, &num2, &num3);
	if (num1 < num2)
	{
		min = num1;
		if (min > num3)
			min = num3;
	}
	else
	{
		min = num2;
		if (min > num3)
			min = num3;
	}
	printf("���� ���� ������ %d�Դϴ�.\n", min);
	*/
/*int x, y;
	int result;
	printf("��ǥ(x,y): \n");
	scanf_s("%d %d", &x, &y);
	if (x > 0 && y > 0)
		result = 1;
	if (x < 0 && y > 0)
		result = 2;
	if (x < 0 && y < 0)
		result = 3;
	if (x > 0 && y < 0)
		result = 4;
	printf("%d ��и�", result);*/
/*int sum = 0;
for (int i = 1; i <= 100; i++)
{
	if (i % 3 == 0)
		sum += i;
}
printf("1���� 100���� ��� 3�� ����� ���� %d�Դϴ�.", sum);*/
/*int num;
printf("������ �Է��Ͻÿ�: ");
scanf_s("%d", &num);
printf("���: ");
for (int i = 1; i <= num; i++)
{
	if (num % i == 0)
		printf("%d ", i);
}*/
/*int i, j, k;
for (i = 1; i < 8; i++)
{	
	for (j = 7; j > i; j--)
		{
			printf(" ");
		}
		for (k = 1; k <= i; k++)
		{
			printf("*");
		}
		printf("\n");
}
*/
/*	int num;
	printf("������ �Է��Ͻÿ�: ");
	scanf_s("%d", &num);
	for (int i = 0; i < num; i++) 
	{
		for (int j = 1; j <=i+1; j++)
			printf("%d ", j);
		printf("\n");
	}*/
/*	int i, j;
	for (i = 2; i <= 100; i++) {
		for (j = 2; j < i; j++)
		{
			if (i % j == 0)
				break;
		}
		if (i == j)
			printf("%d ", i);
	}
	*/
/*int i;
	int sum = 0;
	int result = 0;
	for (i = 1;; i++)
	{
		sum += i;
		if (sum > 10000) {
			result = sum - i;
			break;
		}
	}
	printf("1���� %d������ ���� %d�Դϴ�.\n", i-1, result);*/
/*	int num;
	int a = 0;
	int b = 1;
	int c;
	printf("���° �ױ��� ���ұ��?: ");
	scanf_s("%d", &num);
	printf("%d, %d, ", a, b);
	for (int i = 1; i < num ; i++)
	{
		c = a + b;
		a = b;
		b = c;
		printf("%d, ", c);
	}
	*/
return 0;
}
